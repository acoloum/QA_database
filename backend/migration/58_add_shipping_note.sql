-- 出貨檢驗數據：新增「備註」欄位。
--
-- 背景：出貨檢驗編輯視窗只能填量測數值與訂單號碼，遇到外觀異常、特採放行、
-- 客戶指定條件等需要文字說明的狀況時無處記錄，只能寫在紙本或口頭交代。
--
-- 欄位為可空 TEXT，既有紀錄一律留 NULL；服務層另有 500 字上限，
-- 資料庫端不加約束，避免歷史資料在補寫時被擋下。
BEGIN;

ALTER TABLE "出貨檢驗數據"
    ADD COLUMN IF NOT EXISTS "備註" TEXT;

COMMIT;
