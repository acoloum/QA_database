# 廠商公差管理系統 - 使用說明

## 系統架構

### 資料庫設計
- **廠商公差主檔** (`dbo.廠商公差主檔`)
  - 儲存公差基本資訊（材質、規格、廠商、備註）
  - 追蹤建立/更新人員和時間

- **廠商公差明細檔** (`dbo.廠商公差明細檔`)
  - 儲存詳細的測量項目和公差範圍
  - 支援多種測量項目（外徑、內徑、厚度、硬度等）

### API 端點
1. `GET /api/tolerance/search` - 查詢公差資料
2. `GET /api/tolerance/<id>` - 獲取單筆詳細資料
3. `POST /api/tolerance/add` - 新增公差資料
4. `POST /api/tolerance/update/<id>` - 更新公差資料
5. `POST /api/tolerance/delete/<id>` - 刪除公差資料
6. `GET /api/tolerance/options` - 獲取選項資料
7. `GET /api/tolerance/export` - 匯出 Excel
8. `POST /api/tolerance/import` - 匯入 Excel

## 部署步驟

### 1. 創建資料庫表格
在 SQL Server Management Studio 中執行以下腳本：

```sql
-- 執行檔案位置
C:\Users\bihro\OneDrive\桌面\品保資料庫前後端程式\scripts\create_tolerance_tables.sql
```

或手動執行：
```sql
USE [品保資料庫]
GO

-- 創建主檔
CREATE TABLE [dbo].[廠商公差主檔] (
    [識別碼] INT IDENTITY(1,1) PRIMARY KEY,
    [材質] NVARCHAR(50) NOT NULL,
    [規格] NVARCHAR(100),
    [廠商ID] INT,
    [備註] NVARCHAR(500),
    [建立日期] DATETIME DEFAULT GETDATE(),
    [建立人員] INT,
    [更新日期] DATETIME,
    [更新人員] INT,
    CONSTRAINT [FK_廠商公差主檔_廠商] FOREIGN KEY ([廠商ID]) 
        REFERENCES [dbo].[廠商資料]([識別碼]),
    CONSTRAINT [FK_廠商公差主檔_建立人員] FOREIGN KEY ([建立人員]) 
        REFERENCES [dbo].[品管人員]([識別碼]),
    CONSTRAINT [FK_廠商公差主檔_更新人員] FOREIGN KEY ([更新人員]) 
        REFERENCES [dbo].[品管人員]([識別碼])
)

-- 創建明細檔
CREATE TABLE [dbo].[廠商公差明細檔] (
    [識別碼] INT IDENTITY(1,1) PRIMARY KEY,
    [主檔ID] INT NOT NULL,
    [測量項目] NVARCHAR(50) NOT NULL,
    [測量位置] NVARCHAR(50),
    [尺寸下限] DECIMAL(18,4),
    [尺寸上限] DECIMAL(18,4),
    [公差下限] DECIMAL(18,4),
    [公差上限] DECIMAL(18,4),
    [標準值] DECIMAL(18,4),
    [單位] NVARCHAR(20) DEFAULT N'mm',
    [備註] NVARCHAR(200),
    CONSTRAINT [FK_廠商公差明細檔_主檔] FOREIGN KEY ([主檔ID]) 
        REFERENCES [dbo].[廠商公差主檔]([識別碼]) 
        ON DELETE CASCADE
)

-- 創建索引
CREATE INDEX [IX_廠商公差主檔_材質] ON [dbo].[廠商公差主檔]([材質])
CREATE INDEX [IX_廠商公差主檔_廠商ID] ON [dbo].[廠商公差主檔]([廠商ID])
CREATE INDEX [IX_廠商公差明細檔_主檔ID] ON [dbo].[廠商公差明細檔]([主檔ID])
CREATE INDEX [IX_廠商公差明細檔_測量項目] ON [dbo].[廠商公差明細檔]([測量項目])
```

### 2. 啟動後端服務
```powershell
cd "C:\Users\bihro\OneDrive\桌面\品保資料庫前後端程式"
python app.py
```

### 3. 訪問系統
開啟瀏覽器，訪問：
```
http://localhost:5000/index.html
```

登入後，點擊「廠商公差管理」卡片即可進入系統。

## 功能說明

### 查詢功能
- 支援材質、廠商、規格的篩選
- 可快速找到特定公差資料

### 新增/編輯功能
- 填寫主檔基本資訊（材質、規格、廠商）
- 動態新增/刪除明細行
- 支援多種測量項目和公差範圍

### 查看詳情
- 以卡片形式展示主檔資訊
- 表格形式顯示所有明細資料

### 匯入/匯出
- 匯出：將查詢結果匯出為 Excel 檔案
- 匯入：從 Excel 批量匯入公差資料
  - Excel 格式：材質、規格、廠商名稱、測量項目、測量位置、尺寸下限、尺寸上限、公差下限、公差上限、標準值、單位、備註

## Excel 匯入格式範例

| 材質 | 規格 | 廠商名稱 | 測量項目 | 測量位置 | 尺寸下限 | 尺寸上限 | 公差下限 | 公差上限 | 標準值 | 單位 | 備註 |
|------|------|----------|----------|----------|----------|----------|----------|----------|--------|------|------|
| 6066-T6 | 押出 | 某廠商 | 外徑 | 1 | | | -0.15 | 0.15 | | mm | |
| 6066-T6 | 押出 | 某廠商 | 內徑 | 1 | | | -0.10 | 0.10 | | mm | |
| 6061-T6 | 抽管 | 某廠商 | 厚度 | 1 | | | -0.05 | 0.05 | | mm | |

**注意**：
- 當材質改變時，系統會自動創建新的主檔
- 同一材質的多筆明細會歸屬到同一個主檔

## 資料庫關聯

```
廠商資料 (1) ─── (N) 廠商公差主檔 (1) ─── (N) 廠商公差明細檔
                     ↓
                 品管人員 (建立/更新人員)
```

## 注意事項
1. 材質為必填欄位
2. 刪除主檔時，所有相關明細會自動刪除 (CASCADE)
3. 廠商ID 必須在 `dbo.廠商資料` 中存在
4. 建立/更新人員會自動記錄當前登入用戶

## 疑難排解

### 表格創建失敗
- 確認 `dbo.廠商資料` 和 `dbo.品管人員` 表格已存在
- 確認資料庫權限足夠

### API 調用失敗
- 確認 Token 有效（需登入）
- 檢查後端服務是否正常運行
- 查看瀏覽器控制台的錯誤訊息

### 匯入失敗
- 檢查 Excel 欄位名稱是否正確
- 確認廠商名稱在資料庫中存在
- 確認數值欄位格式正確
